# 🎓 PetrovichCRM / TutorMaster

Современная CRM-система для репетиторов и частных преподавателей. Помогает эффективно управлять расписанием, отслеживать прогресс учеников и анализировать занятия с помощью искусственного интеллекта.

---

## 🚀 Стек технологий

### Frontend
*   **React 19**: Использование новейших возможностей библиотеки для построения UI.
*   **Tailwind CSS**: Современный фреймворк для быстрой и гибкой стилизации.
*   **Google Gemini API**: Интеграция ИИ для автоматического анализа заметок и прогресса учеников.
*   **ES6 Modules & Import Maps**: Загрузка модулей напрямую в браузере без тяжелых бандлеров (Vite/Webpack) в облегченной версии.

### Backend
*   **NestJS**: Мощный Node.js фреймворк для создания надежных серверных приложений.
*   **Prisma ORM**: Типизированная работа с базой данных и легкое управление миграциями.
*   **JWT (JSON Web Tokens)**: Безопасная аутентификация пользователей.
*   **Bcrypt**: Надежное хеширование паролей.

### Database & Infra
*   **PostgreSQL**: Реляционная база данных для хранения данных пользователей и уроков.
*   **Docker & Docker Compose**: Возможность быстрого развертывания всей инфраструктуры одной командой.

---

## 🛠️ Инструкция по локальному запуску (Без Docker)

Этот гид поможет вам запустить CRM систему на вашем компьютере "напрямую".

### 1. Предварительные требования

Перед началом убедитесь, что у вас установлены:
1.  **Node.js** (версия 20.x или 22.x).
2.  **PostgreSQL** (версия 15 или выше).
3.  **npm** (идет в комплекте с Node.js).

---

### 2. Настройка Базы Данных (PostgreSQL)

Вам нужно создать пустую базу данных:

1. Откройте **pgAdmin 4** или используйте терминал.
2. Создайте базу данных с именем: `petrovich_crm`.
   ```sql
   CREATE DATABASE petrovich_crm;
   ```

---

### 3. Настройка Бэкенда (NestJS)

1. Перейдите в папку с бэкендом:
   ```bash
   cd backend
   ```
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Создайте файл `.env` в папке `backend/` и настройте его:
   ```env
   PORT=3000
   # Замените ВАШ_ПАРОЛЬ и данные подключения
   DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/petrovich_crm?schema=public"
   JWT_SECRET="random_secure_string_here"
   # Ключ от Google AI Studio (https://aistudio.google.com/)
   API_KEY="ВАШ_GEMINI_API_KEY"
   ```
4. Синхронизируйте структуру таблиц:
   ```bash
   npx prisma generate
   npx prisma migrate dev --name init
   ```
5. Запустите сервер:
   ```bash
   npm run start:dev
   ```
   *Сервер будет доступен по адресу: http://localhost:3000*

---

### 4. Настройка Фронтенда

Фронтенд работает как статическое приложение, но для корректной работы ESM-модулей его нужно запустить через любой локальный веб-сервер.

1. Установите `serve` (если не установлен):
   ```bash
   npm install -g serve
   ```
2. Запустите сервер из **корня проекта** (там, где лежит `index.html`):
   ```bash
   serve -l 3001
   ```
3. Откройте браузер: [http://localhost:3001](http://localhost:3001).

---

## 🆘 Решение частых проблем

### Ошибка "Failed to fetch" при регистрации
*   Убедитесь, что бэкенд запущен на порту 3000.
*   Проверьте, что в консоли бэкенда нет ошибок подключения к PostgreSQL.

### Ошибка Prisma (P1001)
*   Проверьте правильность `DATABASE_URL` в `.env`.
*   Убедитесь, что служба PostgreSQL запущена на вашем ПК.

### AI функции не работают
*   Убедитесь, что в `.env` указан валидный `API_KEY` от Google Gemini.
*   Проверьте консоль браузера на наличие ошибок API.

---
🎨 *PetrovichCRM — сделано для учителей, которые ценят свое время.*
