# 🛠️ PetrovichCRM

**PetrovichCRM** — это современная система управления отношениями с клиентами, построенная на передовом стеке технологий. Проект поддерживает развертывание как через Docker, так и классическим способом на локальной машине.

## 🚀 Стек технологий

| Слой | Технология |
| :--- | :--- |
| **Frontend** | [Next.js](https://nextjs.org/) (React, Tailwind CSS) |
| **Backend** | [NestJS](https://nestjs.com/) (Node.js) |
| **ORM** | [Prisma](https://www.prisma.io/) |
| **База данных** | [PostgreSQL](https://www.postgresql.org/) |
| **Инфраструктура** | [Docker](https://www.docker.com/), Docker Compose |

---

## 📂 Структура проекта

*   `backend/` — API-сервер на NestJS.
*   `frontend/` — Пользовательский интерфейс на Next.js.
*   `docker-compose.yml` — Описание сервисов для Docker.

---

## 💻 Запуск без Docker (Локальная разработка)

Если вы хотите запустить проект напрямую на вашей ОС, следуйте этой инструкции.

### 1. Предварительные требования
*   **Node.js**: версия 20.x или выше.
*   **PostgreSQL**: установленный и запущенный сервер БД.
*   **Менеджер пакетов**: `npm` (идет в комплекте с Node.js).

### 2. Настройка Базы Данных
Создайте пустую базу данных в PostgreSQL (например, с именем `petrovich_crm`).

### 3. Настройка и запуск Backend (NestJS)
Перейдите в директорию бэкенда:
```bash
cd backend
```

1.  **Установите зависимости**:
    ```bash
    npm install
    ```

2.  **Настройте переменные окружения**:
    Создайте файл `.env` в папке `backend/`:
    ```env
    PORT=3000
    DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/petrovich_crm?schema=public"
    JWT_SECRET="your_secret_key"
    ```
    *Замените `USER` и `PASSWORD` на ваши данные от PostgreSQL.*

3.  **Сгенерируйте Prisma Client и примените миграции**:
    ```bash
    npx prisma generate
    npx prisma migrate dev --name init
    ```

4.  **Запустите сервер**:
    ```bash
    # Режим разработки
    npm run start:dev
    ```

### 4. Настройка и запуск Frontend (Next.js)
Откройте новое окно терминала и перейдите в директорию фронтенда:
```bash
cd frontend
```

1.  **Установите зависимости**:
    ```bash
    npm install
    ```

2.  **Настройте переменные окружения**:
    Создайте файл `.env.local` в папке `frontend/`:
    ```env
    NEXT_PUBLIC_API_URL=http://localhost:3000
    ```

3.  **Запустите приложение**:
    ```bash
    npm run dev
    ```

---

## 🐳 Запуск через Docker (Быстрый старт)

Если у вас установлен Docker, запуск выполняется одной командой:

1.  Создайте общий `.env` в корне проекта (см. раздел ниже).
2.  Запустите сборку:
    ```bash
    docker-compose up -d --build
    ```
3.  Примените миграции в контейнере:
    ```bash
    docker exec -it petrovich-api npx prisma migrate deploy
    ```

---

## ⚙️ Переменные окружения (.env)

Пример конфигурации для корня проекта (при использовании Docker):

```env
# Параметры БД
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=petrovich_crm

# Секреты приложения
JWT_SECRET=super_secret_key
```

---

## 🌐 Доступ к компонентам

| Сервис | URL |
| :--- | :--- |
| **Frontend** | [http://localhost:3001](http://localhost:3001) |
| **Backend API** | [http://localhost:3000](http://localhost:3000) |
| **Документация Swagger** | [http://localhost:3000/api](http://localhost:3000/api) |

---
🎨 *Разработано с душой для эффективного бизнеса.*