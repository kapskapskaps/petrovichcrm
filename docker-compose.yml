
services:
  crm-app:
    build:
      context: .
      args:
        - API_KEY=${API_KEY}
    ports:
      - "8080:80"
    environment:
      - API_KEY=${API_KEY}
    restart: always

# Instructions:
# 1. Create a .env file with API_KEY=your_key_here
# 2. Run 'docker-compose up --build -d'
